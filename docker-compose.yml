services:
  portfolio:
    image: node:22-alpine
    container_name: portfolio_app
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - '5173:5173'
    command: sh -c "npm ci && npm run dev -- --host"
